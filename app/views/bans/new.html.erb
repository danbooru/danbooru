<div class="c-bans">
  <div class="a-new">
    <h1>New Ban</h1>
    <%= render "form", :ban => @ban %>
  </div>
</div>

<%= render "secondary_links" %>

<% content_for(:page_title) do %>
  New Ban - <%= Danbooru.config.app_name %>
<% end %>
