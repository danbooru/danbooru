<div id="c-bans">
  <div id="a-edit">
    <h1>Edit Ban</h1>

    <%= edit_form_for(@ban) do |f| %>
      <%= f.input :duration, :hint => "in days" %>
      <%= f.input :reason %>
      <%= f.button :submit, :value => "Ban" %>
    <% end %>
  </div>
</div>

<%= render "secondary_links" %>
