<h2>Wiki Page Updates</h2>

<table class="striped w-full">
  <thead>
    <tr>
      <th>User</th>
      <th>Count</th>
    </tr>
  </thead>
  <tbody>
    <% @dashboard.wiki_pages.each do |user, count| %>
      <tr>
        <td><%= link_to_user(user) %></td>
        <td><%= link_to count, wiki_page_versions_path(search: { updater_id: user.id }) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
