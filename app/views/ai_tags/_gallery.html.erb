<%= render(MediaAssetGalleryComponent.new(size: size)) do |gallery| %>
  <% ai_tags.each do |ai_tag| %>
    <% if policy(ai_tag.media_asset).can_see_image? %>
      <% gallery.with_media_asset do %>
        <%= render "ai_tags/preview", ai_tag: ai_tag, media_asset: ai_tag.media_asset, post: ai_tag.media_asset.post, size: gallery.size %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
