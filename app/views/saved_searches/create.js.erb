<% if @saved_search.errors.any? %>
  Danbooru.Notice.error("<%= j @saved_search.errors.full_messages.join(', ') %>");
<% else %>
  Danbooru.Notice.info("Search '<%= j @saved_search.query %>' was saved");
<% end %>
