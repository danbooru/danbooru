<% TagCategory.css_mapping.each do |category,cssmap| %>
  .tag-type-<%= category %> a, a.tag-type-<%= category %> {
    color: <%= cssmap["color"] %>;

    &:link, &:visited {
      color: <%= cssmap["color"] %>;
    }

    &:hover {
      color: <%= cssmap["hover"] %>;
    }
  }
<% end %>
